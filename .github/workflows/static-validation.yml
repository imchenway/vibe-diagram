name: Static validation

on:
  pull_request:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  release-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: python314
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
      - name: Run read-only release verification
        env:
          RELEASE_STATE_DIR: ${{ runner.temp }}/vibe-diagram-release
        run: |
          PYTHONDONTWRITEBYTECODE=1 "${{ steps.python314.outputs.python-path }}" \
            scripts/release_github_skill.py verify \
            --version "$(tr -d '\n' < VERSION)" \
            --state-dir "$RELEASE_STATE_DIR" \
            --json
          git diff --exit-code
          test -z "$(git status --porcelain)"
          unexpected_ignored="$(git ls-files --others --ignored --exclude-standard | sed '/^build\//d')"
          test -z "$unexpected_ignored"
