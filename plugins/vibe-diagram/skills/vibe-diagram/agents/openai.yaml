interface:
  display_name: "vibe-diagram"
  short_description: "Create and deliver canonical self-contained HTML diagrams."
  default_prompt: "Use $vibe-diagram to create, validate, and deliver a canonical self-contained HTML diagram for this request."

policy:
  allow_implicit_invocation: true
